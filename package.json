{
  "name": "wanderwell",
  "version": "0.0.1",
  "description": "File Explorer interface in vanilla JavaScript",
  "author": "Lance Turri",
  "repository": "https://github.com/LanceTurri/wanderwell",
  "license": "MIT",
  "keywords": [
    "typescript",
    "file-explorer",
    "esbuild"
  ],
  "browser": "./public/dist/index.js",
  "scripts": {
    "lint": "eslint src/ --ext .ts",
    "test": "jest",
    "clean": "rm -rf dist build package",
    "generate-listing": "ts-node helpers/generateFileListing.ts",
    "scss": "sass src/styles:public/styles",
    "scss:watch": "sass src/styles:public/styles --watch",
    "start": "npm run esbuild-browser:watch & npx http-server -o",
    "build": "tsc -p tsconfig.json",
    "build-all": "npm run clean && npm run build && npm run esbuild-browser",
    "esbuild-browser": "esbuild src/browser.ts --bundle --minify --sourcemap=external --outfile=public/dist/index.js",
    "esbuild-browser:dev": "esbuild src/browser.ts --bundle --outfile=public/dist/index.js",
    "esbuild-browser:watch": "esbuild src/browser.ts --bundle --watch --outfile=public/dist/index.js"
  },
  "devDependencies": {
    "@types/jest": "^26.0.21",
    "@types/node": "^15.0.1",
    "@typescript-eslint/eslint-plugin": "^4.19.0",
    "@typescript-eslint/parser": "^4.19.0",
    "esbuild": "^0.11.11",
    "eslint": "^7.22.0",
    "http-server": "^14.1.0",
    "jest": "^26.6.3",
    "prettier": "^2.6.2",
    "sass": "^1.51.0",
    "ts-jest": "^26.5.4",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}